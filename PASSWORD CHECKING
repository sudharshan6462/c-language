#include<stdio.h>
#include<string.h>
int main()
{
   char pass[20];
   char ch;
   int i;
   while(ch !='0'){
   	ch=getch();
	   pass[i]=ch;
	   printf("*");
	   i++;
   }
   pass[i-1]='\0';	
   printf("enter the password is %s",pass);
   return 0;
}
